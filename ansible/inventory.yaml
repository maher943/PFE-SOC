---
all:
  children:

    k3s_master:
      hosts:
        master:

    k3s_workers:
      hosts:
        worker:
   # Nouveaux groupes Wazuh
    wazuh_manager:
      hosts:
        localhost: 
          ansible_connection: local 

    wazuh_agent:
      hosts:
        node1:
        master:
        worker:
      
    wazuh_indexer:
      hosts:
        localhost: 
          ansible_connection: local

    wazuh_dashboard:
      hosts:
        localhost: 
          ansible_connection: local
      
        
  vars:
    ansible_user: user-ansible
    ansible_ssh_private_key_file: /home/user-ansible/.ssh/id_ed25519
    ansible_become: true
  