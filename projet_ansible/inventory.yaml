all:
  children:

    k3s_master:
      hosts:
        master:

    k3s_workers:
      hosts:
        worker:

  vars:
    ansible_user: user-ansible
    ansible_ssh_private_key_file: /home/user-ansible/.ssh/id_ed25519
